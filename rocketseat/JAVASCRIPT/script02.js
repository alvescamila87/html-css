// Object

const person = {
    name: "Camila",
    age: 35,
    weight: 58,
    isAdmin: true
}

console.log(person) //imprimir o objeto
console.log(person.age) //imprimir somente a propriedade
console.log(person.name, person.age)
console.log(`${person.name}, com ${person.age} anos e tem ${person.weight} kgs.`)

